console.log(`hello from edit`)
var edit_comment_form = $(`<%= j(render 'form', comment: @comment) %>`);
var new_comment_form = $("#<%= dom_id(@comment.photo) %>_comment_form");
new_comment_form.hide();
new_comment_form.before(edit_comment_form);
